using UnityEngine;

public class Bullet : MonoBehaviour
{   
    Vector2 GoDir; //탄막의 진행방향
    int DMG = 5; //탄막의 데미지
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
